from django.urls import path
from . import views

app_name = 'guardias'

urlpatterns = [
    # URLs básicas para las APIs de guardias
    # path('guardias/', views.GuardiaListView.as_view(), name='guardia-list'),
    # path('guardias/<int:pk>/', views.GuardiaDetailView.as_view(), name='guardia-detail'),
    # path('integrantes/', views.IntegranteListView.as_view(), name='integrante-list'),
    # path('calendario/', views.CalendarioView.as_view(), name='calendario'),
] 